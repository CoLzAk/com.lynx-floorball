{% extends 'ColzakAdminBundle::layout.html.twig' %}
{% block body %}
    <div class="row">
        <div class="col-md-9">
            <h2 class="clzk-admin-title">Nouvelle catégorie</h2>
        </div>
        <div class="col-md-3">
            <a href="{{ path('colzak_admin_category_delete', {'id': category.id}) }}" class="btn btn-block btn-danger">Annuler</a>
        </div>
    </div>
    <hr>
    {{ form_start(form, {'attr' : {'class':'form-horizontal'}}) }}
    	{{ form_rest(form) }}
    	
    	<div class="form-group">
    		<div class="col-md-3 col-md-offset-9">
    			<button type="submit" class="btn btn-primary btn-block">Valider</button>
    		</div>
    	</div>
    {{ form_end(form) }}
{% endblock %}